var parallaxContainer = document.getElementById('parallax'),
	layer = parallaxContainer.lasrElementChild;

var moveLayers = function(e) {
	console.log('e');
}

window.addEventListener('mousemove', moveLayers);